@model BurialModel
@{
    ViewData["Title"] = "Burial";
}
    <div class="container">
        <div class="row">
            <div class="col-12" style="font-size:larger;">
                @Html.Raw(Model.burialtext)
            </div>
        </div>
    </div>