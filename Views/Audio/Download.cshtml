@model DownloadModel

@{ ViewData["Title"] = "Audio"; }

<div class="container-fluid">
    <div class="d-block">
        <div class="d-flex flex-row">
            @Html.Raw(Model.link_html)
        </div>
    </div>
</div>
</div>